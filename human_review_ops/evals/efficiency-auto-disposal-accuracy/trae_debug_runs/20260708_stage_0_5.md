# 阶段 0.5 TRAE 调试验证记录

## 基础信息

- 调试日期：2026-07-08
- 调试对象：`人审运营智能体 / human-review-operator`
- 调试模式：`debug_only`
- 样板场景：`efficiency-auto-disposal-accuracy`
- 记录文件：`20260708_stage_0_5_results.jsonl`

## 验证结论

阶段 0.5 的可自动验证项已通过，可进入阶段 1 的“感知 + 分析”最小链路开发。

本轮确认：

- TRAE 智能体列表中已存在自定义智能体「人审运营智能体 / human-review-operator」。
- 根目录场景包 `human_review_ops/references/scenarios/efficiency-auto-disposal-accuracy/` 可读取。
- 四类 Skill 内调试快照均存在，可作为 TRAE 跨目录读取异常时的回退路径。
- 6 条样例均完成调试记录，其中包含 3 条正例、2 条反例、1 条低信息量样例。
- 调试阶段未调用真实 Tool/MCP/CLI，未发送真实通知，未写入线上状态。

## UI 验证说明

通过 Computer Use 观察到 TRAE CN 正在运行，并在「智能体」页面看到自定义智能体：

- 名称：`人审运营智能体`
- 标识：`human-review-operator`

配置齿轮入口未能通过 UI 自动化稳定展开，因此本轮没有修改 TRAE 配置。该限制不影响阶段 0.5 的样例路由和文件读取验证，但阶段 1 开始前建议人工打开该智能体配置，确认四类 Skill 已绑定或可被当前 Agent 检索。

## 样例结果

| 样例 | 输入 | 期望 | 实际 | 结论 |
| --- | --- | --- | --- | --- |
| `EADA-P-001` | 分析一下自动处置准确率为什么下降 | 命中自动处置准确率场景，进入 `query_only` | 命中 `efficiency-auto-disposal-accuracy`，进入 `query_only`，输出 QueryPlan 和 source_footer | 通过 |
| `EADA-P-002` | 按策略维度看自动处置准确率 | 命中自动处置准确率场景，进入 `query_only` | 命中 `strategy` 维度，输出 QueryPlan 和 source_footer | 通过 |
| `EADA-P-003` | 自动处置准确率预警应该找谁 | 命中自动处置准确率场景，进入 `owner_lookup_only` | 读取 Owner 路由优先级，输出 routing_evidence 和 confidence | 通过 |
| `EADA-N-001` | 质检准确率下降了 | 不应误命中自动处置准确率场景 | 识别为排除意图，要求澄清或切换质量场景 | 通过 |
| `EADA-N-002` | 底线事故数上升了 | 不应误命中自动处置准确率场景 | 识别为排除意图，要求澄清或切换底线事故场景 | 通过 |
| `EADA-L-001` | 这个策略怎么了 | 信息不足，不应查询 | 要求补充指标，不生成 QueryPlan，不查询数据 | 通过 |

## 文件读取验证

优先读取根目录完整场景包：

- `scenario_manifest.md`
- `metric_contract.md`
- `dataset_reference.md`
- `analysis.md`
- `owner_routing.md`
- `sla.md`
- `examples.md`

回退快照目录已确认存在：

- `human_review_ops/skills/perception/references/scenarios/`
- `human_review_ops/skills/analysis/references/scenarios/`
- `human_review_ops/skills/notification/references/scenarios/`
- `human_review_ops/skills/resolution/references/scenarios/`

本轮结果：根目录读取通过，未触发回退。

## 权限边界验证

- Tool/MCP/CLI 调用：无。
- 是否只读：是。
- 是否触发写入：否。
- 是否发送真实通知：否。
- 是否写入线上状态：否。

## 后续动作

阶段 1 开始前需要补充：

- 在 TRAE UI 中人工打开「人审运营智能体」配置，确认 Skill 绑定状态。
- 将 `metric_contract.md` 中的指标 Owner、数据 Owner、业务解释 Owner 从“待补充”替换为真实治理信息。
- 接入 mock / 只读 Tool 后，继续保持 `debug_only`，先验证 tool_call_record，再考虑真实工具权限。
